.row
  - @projects.each do |project|
    .col-sm-6.col-md-4.col-lg-3
      %p= link_to image_tag(get_main_image(project, :medium)), get_main_image(project, :original), class: 'image-popup-no-margins', title: project.description
      %p.text-center= link_to project.name, project.url
